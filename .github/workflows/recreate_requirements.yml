name: recreate requirement.lock

on:
  workflow_dispatch:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: make requirement.txt
      run: |
        git config --local user.email "GitHubAction@kemu.site"
        git config --local user.name "GitHubAction"
        pip install -r requirements.txt
        rm requirements.lock
        git add .
        git commit -m "remove requirements.lock"
        make genlock
        git add .
        git commit -m "create requirements.lock"
        git push origin master -f
